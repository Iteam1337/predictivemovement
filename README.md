# Test Mapbox

## Prerequisites

Create a `.env` in the root of the project with

```
MAPBOX_TOKEN="your-mapbox-token"
```

## Run Project

```sh
npm install
npm start
# in another tab
npm run server
```

Open http://localhost:3000
